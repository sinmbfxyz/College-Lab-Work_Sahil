#include<stdio.h>
void main(){
	int a;
	char b;float c;
	double d;
	printf("storage size for int data type:%d\n",size of (a));
		printf("storage size for int data type:%d\n",size of (b));
			printf("storage size for int data type:%d\n",size of (c));
				printf("storage size for int data type:%d\n",size of (d));
	
}
